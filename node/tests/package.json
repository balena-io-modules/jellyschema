{
    "name": "node-integration-tests",
    "scripts": {
        "test": "jest --env node"
    },
    "dependencies": {
        "jellyschema": "file:../../target/npm/pkg"
    },
    "devDependencies": {
        "jest": "^23.6.0"
    }
}
