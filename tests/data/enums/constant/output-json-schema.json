{
    "$$version": 1,
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "additionalProperties": false,
    "$$order": [
        "string",
        "number",
        "integer",
        "hostname",
        "boolean",
        "port",
        "date",
        "email"
    ],
    "required": [
        "string",
        "number",
        "integer",
        "hostname",
        "boolean",
        "port",
        "date",
        "email"
    ],
    "title": "Const test",
    "properties": {
        "string": {
            "type": "string",
            "enum": [
                "fincm3"
            ]
        },
        "number": {
            "type": "number",
            "enum": [
                -1.2
            ]
        },
        "integer": {
            "type": "integer",
            "enum": [
                1
            ]
        },
        "hostname": {
            "type": "string",
            "format": "hostname",
            "enum": [
                "www.example.com"
            ]
        },
        "boolean": {
            "type": "boolean",
            "enum": [
                false
            ]
        },
        "port": {
            "type": "integer",
            "enum": [
                1337
            ]
        },
        "date": {
            "type": "string",
            "format": "date",
            "enum": [
                "1970-01-01"
            ]
        },
        "email": {
            "type": "string",
            "format": "email",
            "enum": [
                "email@example.com"
            ]
        }
    }
}
