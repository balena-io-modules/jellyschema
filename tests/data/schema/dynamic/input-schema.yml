version: 1
title: Dynamic object.
properties:
  - extendedRules:
      type: object
      title: Rules
      properties:
        - name:
            type: string
            title: Rules set name
      keys:
        title: Priority
        type: string
        pattern: ^[0-9]+$
      values:
        title: Rule
        type: string
