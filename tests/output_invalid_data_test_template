
    #[test]
    fn {name}() {{
        let input_schema : Result<Schema, _> = Schema::from_str(
            include_str!("{path}/input-schema.yml"));

        if input_schema.is_err() {{
            return;
        }}
    }}
